spring:
  h2:
    console:
      enabled: true
      path: "/h2-console"

  datasource:
    url: jdbc:h2:mem:db

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        enable_lazy_load_no_trans: true

locale: en_US
