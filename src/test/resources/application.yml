spring:
  h2:
    console:
      enabled: true
  jpa:
    generate-ddl: false

    hibernate:
      ddl-auto: none
  datasource:
    initialize: true
    schema:
    data:

application:
  rememberKey: "RememberMeApplicationKey"
  locale: "ru_RU"
  strength: 4
